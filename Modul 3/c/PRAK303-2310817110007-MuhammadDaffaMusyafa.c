#include<stdio.h>

int main(){
    int nilai;
    scanf("%g", &nilai);
    if (nilai < 0)
    {
       printf("Negatif");
    }
    else if (nilai > 0)
    {
        printf("Positif");
    }
    else if (nilai = 0)
    {
       printf("nol");
    }
    
}